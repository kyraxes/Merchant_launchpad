# Merchant Launchpad — V1 产品与开发计划

> 状态：可运行 Mock 原型  
> 日期：2026-09-11  
> 定位：运行在 LINE MINI App 中的泰国小商家上线工具，不是外卖平台，也不是消费者点评平台。

## 1. V1 要解决的问题

许多泰国小餐厅和小商店没有专人处理 Google Maps、网站和宣传素材。V1 让商家只提交一份基础资料，然后完成三件相互独立的事情：

1. **把店铺上线 Google Maps**：查找已有条目、整理资料，并引导店主完成认领和验证。
2. **创建自己的店铺网站**：在我们的主网站中生成该商家的独立公开网页，作为可被 Google 收录的 SEO 页面。
3. **生成商品海报**：从店铺资料和商品菜单中选择内容，一键生成可下载、分享或打印的海报。

```text
LINE MINI App（商家操作端）
│
├── 店铺资料 Merchant Passport（只填一次）
│
├── Google Maps 上线助手
├── 店铺网站创建器 ──────> 主网站中的公开店铺网页
└── 商品海报生成器
```

只有“创建店铺网站”会产生额外公开网页。Google Maps 与海报功能都在 LINE MINI App 内完成，不需要各自建设网站。

## 2. 商家最少输入

首轮建档尽量只要求：

- 店铺名称；
- 店铺类别；
- 地址或地图定位；
- 电话和可选 LINE ID；
- 营业时间；
- 店招照片；
- 菜单照片；
- 商品照片；
- 对系统识别结果的一次确认。

后续接入 OCR 和 LLM 后，系统从店招、菜单和定位中提取并生成中、泰、英三语资料。商家不重复填写三个工具各自的表单。

## 3. LINE MINI App 页面

| 路径 | 页面 | 作用 |
|---|---|---|
| `/{locale}` | 商家工作台 | 显示资料完成度和三个工具的状态 |
| `/{locale}/profile` | 店铺资料 | 一次建立和修改 Merchant Passport |
| `/{locale}/google` | Google Maps 助手 | 查重、资料确认、验证引导 |
| `/{locale}/website` | 店铺网站创建器 | 查看生成内容、状态和预览入口 |
| `/{locale}/posters` | 商品海报 | 选择商品、语言、尺寸、风格和二维码 |

`locale` 支持 `th`、`en` 和 `zh`。原型用普通浏览器模拟 LINE MINI App 的手机界面，正式接入时再加入 LIFF 登录和 LINE 用户身份。

## 4. 公开 SEO 页面

公开页面属于我们的主网站，不属于 LINE MINI App 界面：

```text
/th/stores/{merchant-slug}
/en/stores/{merchant-slug}
/zh/stores/{merchant-slug}
```

每个页面包含：

- 真实店名、类别、区域和店铺介绍；
- 地址、营业时间、电话和 Google Maps 导航；
- 可被搜索引擎读取的 HTML 菜单、商品和价格；
- 独立 title、description 和 canonical；
- 中泰英 hreflang；
- `Restaurant` 或适当的 `LocalBusiness` 结构化数据；
- Sitemap；
- 移动端布局。

V1 不制作消费者商家目录、附近商家搜索、评分榜单或推荐流。Mock 页面必须保持 `noindex`，只有经过商家确认的真实页面才允许被 Google 收录。

## 5. Google Maps 功能边界

V1 可以：

- 根据店名和位置查询可能存在的 Google Maps 条目；
- 展示创建、认领和资料完整度步骤；
- 生成商家资料草稿；
- 引导店主登录 Google 并完成验证；
- 提醒 Google 与网站中的名称、地址、电话和营业时间保持一致。

V1 不可以：

- 绕过 Google 登录、所有权认领或验证；
- 伪装成已经接入 Google Business Profile API；
- 保证固定排名、前三名或推荐结果；
- 购买、奖励或筛选评价。

## 6. 海报功能边界

商家从已经录入的菜单中选择商品，然后选择：

- 中、泰、英语言；
- 社交媒体方形、Story 或 A4 尺寸；
- 视觉主题；
- 可选二维码目标：店铺网站、Google Maps、LINE 或不添加二维码。

原型使用 Canvas 和占位商品图生成 PNG。正式版本可接入图片生成 API，但文字、价格、Logo 和二维码应由程序确定性叠加，避免 AI 生成错误文字。V1 不做海报访问追踪。

## 7. 明确不做

- 外卖、订单、骑手或配送；
- 配送费、优惠券或外卖活动；
- 支付、POS 或记账；
- 消费者点评、评分或附近推荐；
- 趣味点餐、抽奖或顾客反馈；
- Google 排名承诺；
- 虚假 Google API 集成；
- 未授权发布真实商户信息。

## 8. Mock 验收标准

- 首页是商家工作台，不是消费者找店首页；
- 工作台只展示 Google Maps、店铺网站、商品海报三个功能；
- 店铺资料页可以编辑并模拟保存；
- Google 模块只展示当前商户的上线步骤；
- 网站模块能够进入独立公开店铺页面；
- 公开页面不显示配送、优惠券、虚构评分或排名；
- 海报能够切换商品、语言、尺寸、风格和二维码目标；
- 海报能够下载 PNG；
- 中文、泰文、英文路径都可以打开；
- 手机端和桌面端布局可用；
- TypeScript 检查和正式构建通过。

## 9. 从 Mock 到线下试点

1. 找 3–5 家明确授权的真实商户；
2. 让商家用店招照片、菜单照片、定位、电话和营业时间完成建档；
3. 人工校对 OCR 和三语生成结果；
4. 分别观察三个任务是否能独立完成；
5. 由店主本人完成 Google 登录和验证；
6. 商家确认公开网站内容后再允许收录；
7. 让商家实际下载并发布一张海报；
8. 记录完成时间、放弃步骤和需要人工协助的次数；
9. 再决定优先自动化 Google、SEO 内容还是海报生成。

## 10. 当前技术实现

- Next.js、React、TypeScript；
- 本地 Mock 商户数据，无数据库也可运行；
- Canvas 生成海报；
- QR code 生成；
- SEO metadata、JSON-LD、canonical、hreflang、Sitemap 和 robots；
- Mock 模式默认禁止搜索引擎收录。

运行与检查：

```powershell
pnpm dev
pnpm typecheck
pnpm build
```

环境变量：

```text
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_MOCK_MODE=true
```

只有使用经过确认的真实商户数据后，才能把 `NEXT_PUBLIC_MOCK_MODE` 设置为 `false`。

当前 Mock 版本部署在 Netlify。构建时优先使用 `NEXT_PUBLIC_SITE_URL`，未设置时自动读取 Netlify 提供的 `URL` 或 `DEPLOY_PRIME_URL`，避免 canonical、JSON-LD 和 Sitemap 指向 localhost。

## 11. LINE LIFF 接入

- LIFF ID：`2011546347-Bs9vtzj1`（这是公开的前端标识，不是 Channel Secret）；
- LIFF Endpoint URL 必须设置为 `https://symphonious-longma-ac0c5e.netlify.app/`；
- Scopes 使用 `profile` 和 `openid`；
- LINE 内打开时自动读取登录状态和用户昵称；
- 普通浏览器不会被强制登录，仍可作为 Mock 演示，并提供“在 LINE 中打开”的入口；
- 根路径会先完成 LIFF 初始化，再进入泰文首页，避免服务器重定向丢失 LIFF 启动参数。

V1 只在浏览器中显示 LINE 连接状态，不把用户资料上传或保存。正式绑定商户账号时，前端只向服务端发送 LINE token，由服务端向 LINE 验证；不能信任前端直接提交的用户 ID 或昵称。

## 12. 可上传的交互 Mock

店铺资料页现已支持：

- 导入测试商户 JSON；
- 从相册选择图片或直接调用手机相机；
- 店招、菜单和商品图片预览；
- 上传前在浏览器中压缩图片；
- 将草稿保存在当前设备的 Local Storage；
- 刷新后恢复草稿；
- 将店名、分类、地址和照片复用到首页、Google 模块、网站预览和海报；
- 导出当前草稿 JSON；
- 一键重置 Mock 数据。

测试数据位于 `public/test-data/`。这些数据完全虚构，仅用于流程测试。Local Storage 数据不会发送给团队，换设备或清除浏览器数据后可能消失；接入数据库之前，不能把当前版本描述为已经收集商户数据。

## 13. LINE 主动入口

`/launch` 页面提供 LIFF 二维码、复制链接和浏览器预览入口。当前是 LIFF App，不会出现在 LINE MINI App 的搜索或目录中。正式试点时应建立 LINE Official Account，并把 LIFF URL 放入：

- 添加好友后的欢迎消息；
- 聊天页面底部 Rich Menu；
- 店内桌贴、海报或销售人员展示的二维码。

Rich Menu 在手机 LINE 的 Official Account 聊天中显示，不能把普通 LIFF App 变成可搜索的 LINE MINI App。
