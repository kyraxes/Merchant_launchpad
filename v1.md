# Merchant Launchpad — V1 产品与开发计划

> 状态：可运行服务器数据原型
> 日期：2026-09-11  
> 定位：当前运行在 LINE LIFF App 中的泰国小商家上线工具，未来取得资格后可迁移到 LINE MINI App；它不是外卖平台，也不是消费者点评平台。

## 1. V1 要解决的问题

许多泰国本地小商户——包括餐厅、零售店、服装店、美容店、维修店和个人服务商——没有专人处理 Google Maps、网站和宣传素材。V1 让商家只提交一份基础资料，然后完成三件相互独立的事情：

1. **把店铺上线 Google Maps**：查找已有条目、整理资料，并引导店主完成认领和验证。
2. **创建自己的店铺网站**：在我们的主网站中生成该商家的独立公开网页，作为可被 Google 收录的 SEO 页面。
3. **生成商品或服务海报**：从商户资料和商品/服务列表中选择内容，一键生成可下载、分享或打印的海报。

```text
LINE LIFF App（商家操作端）
│
├── 店铺资料 Merchant Passport（只填一次）
│
├── Google Maps 上线助手
├── 店铺网站创建器 ──────> 主网站中的公开店铺网页
└── 商品海报生成器
```

只有“创建店铺网站”会产生额外公开网页。Google Maps 与海报功能都在 LINE LIFF App 内完成，不需要各自建设网站。

## 2. 商家最少输入

首轮建档尽量只要求：

- 店铺名称；
- 店铺类别；
- 地址或地图定位；
- 电话和可选 LINE ID；
- 营业时间；
- 店招照片；
- 商品、服务、菜单或价目表照片；
- 商品照片；
- 对系统识别结果的一次确认。

后续接入 OCR 和 LLM 后，系统从门店、商品/服务资料和定位中提取并生成中、泰、英三语资料。商家不重复填写三个工具各自的表单。

## 3. LINE LIFF App 页面

| 路径 | 页面 | 作用 |
|---|---|---|
| `/{locale}` | 商家工作台 | 显示资料完成度和三个工具的状态 |
| `/{locale}/profile` | 店铺资料 | 一次建立和修改 Merchant Passport |
| `/{locale}/google` | Google Maps 助手 | 查重、资料确认、验证引导 |
| `/{locale}/website` | 店铺网站创建器 | 查看生成内容、状态和预览入口 |
| `/{locale}/posters` | 商品海报 | 选择商品、语言、尺寸、风格和二维码 |

`locale` 支持 `th`、`en` 和 `zh`。当前原型已经接入 LIFF 登录环境和 LINE 用户资料；普通浏览器没有商户身份，只能查看空工作台和公开页面，不能注册新商户。

## 4. 公开 SEO 页面

公开页面属于我们的主网站，不属于 LINE LIFF App 界面：

```text
/th/stores/{merchant-slug}
/en/stores/{merchant-slug}
/zh/stores/{merchant-slug}
```

每个页面包含：

- 真实店名、类别、区域和店铺介绍；
- 地址、营业时间、电话和 Google Maps 导航；
- 可被搜索引擎读取的 HTML 商品、服务、菜单和价格；
- 独立 title、description 和 canonical；
- 中泰英 hreflang；
- 默认使用通用 `LocalBusiness` 结构化数据；已确认具体行业后再使用 `Restaurant`、`ClothingStore`、`BeautySalon` 等更准确类型；
- Sitemap；
- 移动端布局。

V1 不制作消费者商家目录、附近商家搜索、评分榜单或推荐流。Mock 页面必须保持 `noindex`，只有经过商家确认的真实页面才允许被 Google 收录。

## 5. Google Maps 功能边界

V1 可以：

- 根据店名和位置查询可能存在的 Google Maps 条目；
- 展示创建、认领和资料完整度步骤；
- 生成商家资料草稿；
- 引导店主登录 Google 并完成验证；
- 提醒 Google 与网站中的名称、地址、电话和营业时间保持一致。

V1 不可以：

- 绕过 Google 登录、所有权认领或验证；
- 伪装成已经接入 Google Business Profile API；
- 保证固定排名、前三名或推荐结果；
- 购买、奖励或筛选评价。

## 6. 海报功能边界

商家从已经录入的商品或服务列表中选择内容，然后选择：

- 中、泰、英语言；
- 社交媒体方形、Story 或 A4 尺寸；
- 视觉主题；
- 可选二维码目标：店铺网站、Google Maps、LINE 或不添加二维码。

原型使用 Canvas 和占位商品图生成 PNG。正式版本可接入图片生成 API，但文字、价格、Logo 和二维码应由程序确定性叠加，避免 AI 生成错误文字。V1 不做海报访问追踪。

## 7. 明确不做

- 外卖、订单、骑手或配送；
- 配送费、优惠券或外卖活动；
- 支付、POS 或记账；
- 消费者点评、评分或附近推荐；
- 趣味点餐、抽奖或顾客反馈；
- Google 排名承诺；
- 虚假 Google API 集成；
- 未授权发布真实商户信息。

## 8. 验收标准

- 首页是商家工作台，不是消费者找店首页；
- 工作台只展示 Google Maps、店铺网站、商品海报三个功能；
- 店铺资料页可以编辑服务器中的已提交资料；
- Google 模块只展示当前商户的上线步骤；
- 网站模块能够进入独立公开店铺页面；
- 公开页面不显示配送、优惠券、虚构评分或排名；
- 海报能够切换商品、语言、尺寸、风格和二维码目标；
- 海报能够下载 PNG；
- 中文、泰文、英文路径都可以打开；
- 手机端和桌面端布局可用；
- TypeScript 检查和正式构建通过。

## 9. 从 Mock 到线下试点

1. 找 3–5 家明确授权的真实商户；
2. 让商家用经营场所照片、商品/服务资料、定位、电话和营业时间完成建档；
3. 人工校对 OCR 和三语生成结果；
4. 分别观察三个任务是否能独立完成；
5. 由店主本人完成 Google 登录和验证；
6. 商家确认公开网站内容后再允许收录；
7. 让商家实际下载并发布一张海报；
8. 记录完成时间、放弃步骤和需要人工协助的次数；
9. 再决定优先自动化 Google、SEO 内容还是海报生成。

## 10. 当前技术实现

- Next.js、React、TypeScript；
- Netlify Blobs 站点级持久化商户数据；
- Canvas 生成海报；
- QR code 生成；
- SEO metadata、JSON-LD、canonical、hreflang、Sitemap 和 robots；
- Mock 模式默认禁止搜索引擎收录。

运行与检查：

```powershell
pnpm dev
pnpm typecheck
pnpm build
```

环境变量：

```text
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_MOCK_MODE=true
NEXT_PUBLIC_LIFF_ID=2011546347-Bs9vtzj1
LINE_LOGIN_CHANNEL_ID=2011546347
ADMIN_API_KEY=使用长随机字符串
```

只有使用经过确认的真实商户数据后，才能把 `NEXT_PUBLIC_MOCK_MODE` 设置为 `false`。

当前 Mock 版本部署在 Netlify。构建时优先使用 `NEXT_PUBLIC_SITE_URL`，未设置时自动读取 Netlify 提供的 `URL` 或 `DEPLOY_PRIME_URL`，避免 canonical、JSON-LD 和 Sitemap 指向 localhost。

## 11. LINE LIFF 接入

- LIFF ID：`2011546347-Bs9vtzj1`（这是公开的前端标识，不是 Channel Secret）；
- LIFF Endpoint URL 必须设置为 `https://symphonious-longma-ac0c5e.netlify.app/`；
- Scopes 使用 `profile` 和 `openid`；
- LINE 内打开时自动读取登录状态和用户昵称；
- 普通浏览器不会生成或显示本地商户数据，不能创建商户，并提供“在 LINE 中打开”的入口；
- 根路径会先完成 LIFF 初始化，再进入泰文首页，避免服务器重定向丢失 LIFF 启动参数。

创建商户时，前端只向服务端发送 LINE ID token；服务端调用 LINE 官方验证接口，并使用验证结果中的固定用户 ID 绑定申请。服务端不信任前端直接提交的用户 ID 或昵称。ID token 只保存在页面内存中，不写入 Local Storage、IndexedDB 或数据库。

## 12. 上传与数据保存

- 新商户流程可以从相册选择图片或直接调用手机相机；
- 经营场所、商品、服务或价目表图片会在上传前由浏览器压缩；
- 填写中的内容只存在于当前页面内存，不创建持久化草稿；
- 只有最后点击“提交审核”后，资料和图片才会原子写入服务器；
- 已提交商户可以在资料页修改，修改会直接保存到服务器并重新进入审核；
- 旧版 Local Storage 和 IndexedDB 商户数据在新版首次打开时自动清理。

测试图片位于 `public/test-data/`，只供开发阶段单页视觉检查使用，不会进入商户工作台、后台、公开目录或 Sitemap。

## 13. LINE 主动入口

`/launch` 页面提供 LIFF 二维码、复制链接和浏览器预览入口。当前是 LIFF App，不会出现在 LINE MINI App 的搜索或目录中。正式试点时应建立 LINE Official Account，并把 LIFF URL 放入：

- 添加好友后的欢迎消息；
- 聊天页面底部 Rich Menu；
- 店内桌贴、海报或销售人员展示的二维码。

Rich Menu 在手机 LINE 的 Official Account 聊天中显示，不能把普通 LIFF App 变成可搜索的 LINE MINI App。

## 14. LINE 账号与商户归属

当前版本可以使用测试 LINE 账号和测试商户继续验证完整流程：

- 普通浏览器不加载商户资料；LINE 内只显示当前用户的服务器商户；
- 每个 LINE 账号默认只能创建和管理一家商户；已有商户后不再显示创建入口；
- 商户端只显示当前商户名称、状态和同步按钮，不显示多商户下拉框或管理后台入口；
- 如果未来需要连锁店或代运营多店，必须增加独立申请、人工审核和服务器授权，不能开放为默认自助功能；
- 只有在 LINE 客户端内完成身份验证后，才可以进入新商户建档；普通浏览器只能预览；
- 新商户通过“照片 → 店名与分类 → 地址与联系方式 → 确认提交”四步建档；店名、分类、地址、电话和营业时间是提交审核的最低必填信息；
- 点击“创建店铺”不会立即产生商户记录；只有完成最低必填信息并提交后，才会以“等待审核”状态原子写入商户列表；
- Google、店铺网站预览和海报始终读取当前选中的商户；
- 服务器商户状态包括等待审核、未通过和已发布；未提交表单不是商户记录；
- `/admin` 只提供服务器审核管理，可审核、发布、驳回和重新审核；
- `/{locale}/preview` 在 LINE 内展示当前服务器商户的网站预览。

LINE 用户的商户列表、资料、审核状态、公开目录和固定公开网址全部以服务器数据为唯一来源。

## 15. 真实提交与审核队列

- `POST /api/submissions` 接收最低商户资料和可选压缩图片；
- 请求必须携带 LIFF 的 ID token，服务端通过 LINE Login v2.1 官方接口验证；
- 服务端根据 LINE 用户和客户端提交编号生成稳定申请 ID，重复点击不会创建重复记录；
- 同一 LINE 用户重复提交同一申请会返回已有记录；不同申请会被服务器拒绝，防止绕过界面创建第二家；
- 提交内容和图片保存在 Netlify 站点级持久化存储，部署新版本不会清除；
- `GET /api/admin/submissions` 和 `PATCH /api/admin/submissions/:id` 使用服务器端 `ADMIN_API_KEY` 保护；
- `/admin` 的“真实审核队列”可以读取申请并执行通过、驳回和重新审核；
- 管理密钥只保存在当前浏览器标签页的 Session Storage，不写入网址或长期存储；
- 本地测试可临时设置 `ALLOW_MOCK_LINE_AUTH=true` 并使用 `Bearer mock-line:<测试用户>`；线上必须保持关闭。

### 15.1 数据源规则

- 尚未提交的表单只存在于页面内存；关闭页面后丢弃，不形成草稿或商户记录；
- 一旦提交审核，商户资料、审核状态和公开网页以服务器持久化数据为唯一数据源；
- 生产环境固定使用 Netlify Blobs；只有显式设置 `MERCHANT_STORAGE=memory` 时才使用临时内存，本地开发由 `.env.development` 明确启用；
- 服务端绝不根据 `NODE_ENV` 自动回退到内存，避免云运行环境缺少变量时把临时数据误认为数据库数据；
- 管理后台会显示当前连接的是“持久化服务器数据”还是“临时内存数据”，避免把测试数据误认为正式数据。
- 商户端提供手动同步入口；重新进入 LINE 时也会自动同步服务器审核状态；
- 已提交但未发布的申请删除时会同时清理服务器记录和图片；已发布商户必须先由后台撤回，避免误删公开页面；
- 资料修改直接请求服务器并重新进入审核，不再提供“仅保存到本机”。

Netlify Blobs 适合当前小规模、低写入试点，但不是关系型数据库。需要复杂查询、多人协作、严格事务或大规模数据分析时，应迁移到 PostgreSQL。V1 暂时把“审核通过”定义为“通过并发布”，后续如需预定发布时间或二次检查，再拆分审核和发布状态。
