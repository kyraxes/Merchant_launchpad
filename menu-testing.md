# 第一版线上菜单：测试说明

日期：2026-09-13。本次交付菜单录入、校对、服务器保存与公开发布；尚未接入真实 OCR、购物车、订单或配送。

> 网站模式更新：主程序已脱离 LINE。直接打开网站，点击「账号」注册／登录，首页和底部导航均有菜单入口。后文旧 LINE 测试说明由本段替代。详情见 [网站账号与旧商户迁移](./website-platform.md)。

## 手机测试

新版部署后，在网站首页点击「创建线上菜单」，或底部「菜单」。管理路径是 `/{locale}/menu`。

1. 使用已有商户；只有已通过审核的商户可以公开菜单，其他商户可以先保存校对。
2. 点击「下载测试菜单」，或准备自己的 JPG、PNG、WebP 菜单照片。
3. 选择 1–5 张图片，每张原图不超过 15 MB；系统压缩后显示缩略图。
4. 点击「上传并模拟识别」。所有照片都会返回同一份固定示例，不会真实识别照片内容。
5. 对照原图修改菜名、分类、规格、价格；第二道示例菜故意缺价，需要手动填写。点击原图缩略图可以放大。
6. 点击「保存校对」，看到保存成功后重新进入页面，确认刚才修改还在。
7. 每道菜勾选已核对。缺菜名、缺价、非法价格或未确认时不能发布；本版不支持零价／时价商品。
8. 点击「确认并发布」，然后「打开公开菜单」，或复制链接发给另一台手机。
9. 顾客无需登录即可看到已发布菜单。测试菜单明确标记为示例，不支持点餐，暂不让搜索引擎收录。
10. 再修改菜名并仅保存校对，确认顾客看到的仍是上次发布的版本；重新发布后才改变。
11. 可移除菜品或取消「正在出售」，校对并重新发布后展示变更。
12. 没有未保存修改时可以撤下菜单；原图与校对数据仍在，公开文字菜单不再可访问。

网站入口不受 LINE Channel 的 Developing 状态影响。

## 数据和权限

- 菜单按商户独立保存在现有 Netlify Blobs 站点存储，键为 `menu/{merchantId}`。本轮未引入订单数据库。
- 原图和校对结果只经店主认证接口读取；公开菜单页只读取已发布的文字快照。
- 菜单修改不改变商户审核状态；未审核／被撤回的商户不能公开访问菜单。
- 菜单读取失败时不能用空数据覆盖服务器；两台设备基于同一旧版本保存，一次成功，另一台收到冲突提示。
- 选择新图片后必须先导入，或者移除所选图片，才能继续保存菜品，避免误以为图片已经上传。
- 未点击保存的编辑在页面内存中；刷新或离开前请先保存。照片选择器返回不自动刷新表单。
- 删除商户同时删除菜单数据。撤下菜单只取消公开，不删除校对内容。
- 新版增加独立网站注册与登录，仍需服务器验证账号归属；没有放开匿名写入或管理后台权限。

## 开发验证

```powershell
pnpm typecheck
pnpm build
pnpm test:menu
```

测试脚本需要支持原生 TypeScript 类型擦除的 Node.js（本次使用 Node 24）。它启动绑定 127.0.0.1 的独立随机端口服务器，只使用临时内存和模拟身份，不访问线上数据、不创建真实 LINE 用户，也不需要真实管理员密钥。

可选设置 `PLAYWRIGHT_MODULE` 为本机 Playwright 的 `index.mjs` 绝对路径，并安装可用 Chromium，以执行手机尺寸浏览器验证。现在通过真实网站登录页和菜单页面测试，不再使用模拟 LINE 上下文的组件页面。

覆盖：输入校验、匿名／跨账号／跨来源拒绝、审核要求、缺价与未确认阻止发布、跨请求读取、并发冲突、重新导入保留旧公开版本、撤回与删除清理。可选浏览器验证覆盖三语公开页、手机无横向溢出、管理页门禁、上传压缩、逐项校对、保存刷新恢复、模拟照片选择器返回、发布与再次导入。

本地集成测试覆盖内存存储分支；Netlify Blobs 条件写入采用其 ETag 接口，仍需部署后进行一次双设备持久化与冲突验证。实际手机文件选择和二维码下载也需部署后验收。
