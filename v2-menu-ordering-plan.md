# Merchant Launchpad：菜单识别、线上点餐与履约扩展计划

> 平台方向更新（2026-09-13）：主程序改为独立网站，网站账号代替强制 LINE 身份，顾客公开浏览不变。本文后续阶段中的 LINE 通知改为可选渠道，不能成为接单必要依赖；通知方案在订单阶段重新设计。当前实现见 [网站模式](./website-platform.md)。

> 日期：2026-09-13
> 状态：产品与实施提案；本文件不代表功能已开发或配送已接通。
> 当前版本事实以本次本地代码检查为准；云端部署状态未在本次核验。
> 与旧计划关系：本计划取代 `v1.md` 中“不做订单、配送”的未来范围约束；旧文件继续记录已实现的基础版本。

### 2026-09-13 首次实施记录

已完成本地菜单最小闭环：最多 5 张菜单照片、明确标注的固定 Mock 识别、逐项校对、服务器保存、已审核商户版本发布、独立公开菜单页、分享链接和二维码。管理界面支持中泰英，菜单原文由店主填写。操作与验证见 [菜单测试说明](./menu-testing.md)。

本次未实现真实 OCR、下单、营业日历、收款与配送；后文为后续计划，不应视为已经交付。现有商户基础资料重新审核行为也尚未重构。

## 1. 本次方向调整

从“帮助小商户建立线上展示”扩展为“帮助单店通过自己的线上菜单接单，并选择自取或第三方配送”。

商家仍在 LINE LIFF 中管理业务；消费者通过商家链接、二维码或公开店铺页进入菜单。先围绕商户已有顾客完成交易，不把全城找店、流量推荐、跨店购物车作为本轮前提。

目标闭环：

1. 商家上传菜单照片，系统识别菜名、价格和规格。
2. 商家校对并发布，生成可分享的线上菜单。
3. 顾客选菜、确认价格，选择自取或配送并提交订单。
4. 商家明确接单或拒单，顾客看到处理结果。
5. 自取订单完成取餐；配送订单通过第三方完成交付。
6. 记录订单、异常和必要的运营数据，支持商家持续维护菜单。

基础商户模型继续兼容服装店等行业。首批订单能力只向试点餐饮商户开启；通用商品、服务预约等需求不强行套用餐饮流程。

## 2. 已确认需求与本计划建议

| 项目 | 已确认需求 | 建议默认实现／待决定事项 |
|---|---|---|
| 菜单录入 | OCR 识别照片后生成线上菜单 | 识别后必须由店主校对，不能直接上线价格 |
| 下单 | 顾客可以通过线上菜单点餐 | 第一阶段单店、即时订单；不做预约、堂食桌号、拼单 |
| 履约 | 顾客选择自取或配送 | 先验证真实自取；配送先 Mock，再启用一家服务商 |
| 营业 | 使用营业时间并支持今天不营业 | 周营业表、日期例外、暂停接单、当班接单开关 |
| 身份 | 复用 LINE 商户身份 | 菜单公开浏览；首次真实试点建议顾客提交时 LINE 登录 |
| 收款 | 用户尚未指定 | 自取到店付款；配送资金方案确定后才开放真实配送 |
| 责任 | 希望交易纠纷由商家与顾客解决 | 商家负责商品，配送商负责承运；平台保留技术支持、证据和投诉转交机制，具体法律责任待核实 |
| 收入 | 延续以商户数据积累为核心的方向 | 记录数据质量与成本；未经合法依据或授权不出售可识别个人的数据 |

本文采用上述建议推进设计，正式配送、代收款与法律条款中的未决事项见第 15 节。

## 3. 当前基础与实际缺口

本次检查的主要代码：`lib/types.ts`、`lib/submissions.ts`、`lib/server/submission-store.ts`、`components/ProfileForm.tsx`、`app/[locale]/stores/[slug]/page.tsx`。

| 当前基础 | 可复用内容 | 扩展缺口 |
|---|---|---|
| LINE 商户登录、归属与后台审核 | 商户身份验证、权限模式 | 增加独立顾客会话；顾客不能取得商户权限 |
| 商户资料和图片持久化 | 店名、地址、电话、图片 | 提交模型目前不保存正式可交易菜单 |
| `MenuItem` 展示类型 | 菜名、说明、价格、多语言 | 缺规格、售罄、版本、上架状态和确认来源 |
| 营业时间 | 现有 `hours: string` 展示字段 | 无法可靠计算今天是否营业、跨午夜或临时休店 |
| 公开商户网页 | 菜单入口、店铺信息、SEO | 真实商户页主要展示资料和图片，无购物车和订单 |
| Netlify Blobs | 图片与小规模商户资料 | 当前代码没有订单事务、库存预留和支付账务机制 |
| Google、网站与海报工具 | 继续共用商户资料 | 本轮新增“菜单与接单”，旧工具不作为下单依赖 |

特别需要调整：现在编辑商户资料会重新审核并影响公开状态。进入接单阶段后，要把“基础资料审核”“已发布菜单”“营业与接单状态”分开。售罄、临时休店和接单操作必须即时生效；重要资料修改保留上一份已批准版本，除非管理员主动暂停商户。

## 4. 页面与角色

| 角色 | 主要入口 | 核心操作 |
|---|---|---|
| 店主 | LINE 商家工作台 | 看新订单、确认当班接单、暂停、今天休息 |
| 店主 | 菜单管理 | 上传照片、校对、发布、改价、售罄、上下架 |
| 店主 | 订单详情 | 接单、拒单、预计备餐时间、出餐、核验取餐 |
| 顾客 | 公开店铺页／菜单链接 | 浏览、选择规格、加入单店购物车 |
| 顾客 | 结算与订单页 | 选择自取／配送、提交、看状态、申请取消、联系商家 |
| 平台运营 | 独立管理后台 | 管理试点资格、排查订单、重试失败任务、处理投诉转交 |
| 配送服务商 | 服务端接口 | 报价、创建配送、返回状态、取消与异常 |

浏览菜单不强制 LINE 身份。顾客提交订单时才认证；普通浏览器通过 LINE 登录回到原订单流程，失败时保留当前购物车并给出继续入口。若该入口在试点中明显阻碍转化，再评估手机号验证的访客下单，不能直接放开匿名写订单。

菜单与店铺信息可公开索引；结算、订单、顾客联系方式、商家管理页不进入 sitemap，并设置不索引。隐私保护依靠服务端权限校验，不能依赖 `noindex`。

## 5. OCR 到线上菜单

### 5.1 最小操作流程

上传 1–5 张菜单照片 → 显示上传／识别进度 → 展示分组菜品与原图对照 → 校对高风险字段 → 预览并发布 → 复制链接或下载二维码。

初始只强制确认菜名、价格、必要规格和是否出售。图片、营销说明、翻译可以后补。菜单拍照不能省掉配送地址定位、联系电话等与履约直接有关的信息。

### 5.2 识别和校对规则

- 先提取文字、位置和表格关系，再整理为结构化菜品；不要把整张图片当作最终线上菜单。
- 对价格和规格做确定性校验：如 `60/80` 必须确认对应大小份，不能直接猜一个价格。
- 看不清、缺价格、重复菜品、手写修改、多列错配都标记待确认；缺价不是零元。
- 保存原图、文字定位（供应商支持时）、来源页和修改前后结果，支持店主对照。
- OCR 输出是待验证内容；不能让图片中的指令控制系统操作或发布流程。
- 原语言保留；翻译和补充说明单独标识。系统不推断过敏原、清真、素食等声明。
- 同图重新上传提示重复；重新识别展示差异，不覆盖已发布菜单或店主校对结果。
- 识别失败保留原图并提供重试或手动录入；限制照片数、文件大小、任务并发和单店调用额度。
- 图片压缩同时考虑泰文小字清晰度；比较原图与压缩图识别质量后再定阈值。
- 店主确认通过字段校验后发布一个菜单版本。发布是整体切换，顾客不应看到半份新菜单。

菜单工作版本与过去取消的“未提交商户草稿”不是同一件事。已有商户的识别任务及校对结果可保存在服务器，以免一次识别中断就丢失；未确认版本不对顾客公开，任务设保留期限。

### 5.3 OCR 选型试验

至少准备 20 张已获授权的菜单：泰文、中泰英混排、反光、模糊、多栏、手写价、多规格、重复页。人工标注正确菜名、价格和规格，比较：

- 价格与菜品配对正确率、漏菜率、规格正确率；
- 店主修正耗时和每张图成本；
- 识别耗时及失败恢复；
- 供应商对图片的保留与数据使用条件。

Google Cloud Vision 官方列出泰文 OCR 支持，可作为候选，但不意味着能自动理解所有菜单版式。以实测决定使用 OCR + 结构整理或多模态识别，不先绑定某个模型。[OCR 语言支持](https://docs.cloud.google.com/vision/docs/languages?hl=zh-cn)

## 6. 营业时间与“今天没有营业”

### 6.1 四类独立设置

1. **每周营业表**：周一至周日分别设置，可有午间、晚间两个时段。
2. **日期例外**：某日休息或临时改时间，覆盖那一天的周营业表。
3. **临时暂停**：忙不过来时暂停 30 分钟或暂停到指定时间，也可手动恢复。
4. **当班接单开关**：初期要求店主点“开始接单”，只在当前有效营业时段内生效，过期后不能自动开启新一班。

时间统一按 `Asia/Bangkok` 计算，与电脑、手机或服务器所在时区无关。每个区间明确起止日期／时间，支持 18:00–次日 02:00。

接单需要同时满足：商户已批准且未被暂停、菜单已发布、当前在有效营业时段、当班接单已开启、没有临时暂停、所选自取或配送方式可用。服务端在提交与接单时重新判断。

### 6.2 “今天休息”行为

- 工作台提供明显按钮“今天休息”；点击后立即停止新订单，不进入审核。
- 顾客仍可看菜单，显示“今日休息”；能确定时显示下一营业时间，不能确定就提示等待店主恢复。
- 今日例外在曼谷时间次日 00:00 到期，然后恢复正常排班判断；不会自动打开新的接单班次。
- 若当前班次跨午夜，按钮确认文案明确“停止接单至本班次结束”，暂停同时覆盖午夜后的剩余时段，避免 00:00 意外恢复。
- 当前已接订单继续显示。店主必须逐单完成或发起取消，休店不能静默删除或取消订单。
- 尚未接单的申请取消或拒绝并告知顾客；并发提交由服务端按事务内的最新状态处理。
- 保留最后更新时间和操作者。顾客提交时遇到刚休店，返回明确提示，不产生一个看似成功的订单。

### 6.3 边界规则

- 菜品售罄单独控制，不等于店铺休息；第一版仅做售罄开关，不承诺实时库存数量。
- 自取和配送可分别关闭；配送不能因自取开启而自动开启。
- 停止新单不抹掉已承诺的取餐时间；接近打烊时结合备餐时间与最后接单时间限制新单。
- 暂不做预约订单，休息时不接受明天的订单。

## 7. 自取最小订单闭环

### 7.1 顾客流程

看菜单 → 选择菜品、必要规格、数量 → 选择自取 → 登录并确认联系人／电话 → 显示服务端计算的总价和付款方式 → 提交 → 等待店主确认 → 显示预计取餐时间 → 收到可取餐状态 → 到店付款和取餐。

采用“提交申请、商家确认后成立”的用户提示。第一阶段建议到店付款，不把点击提交显示为“已付款”。

### 7.2 商家流程

新订单提醒 → 查看菜品与备注 → 接单并确认备餐时间，或拒单并选择原因 → 标记可取餐 → 对照取餐码 → 确认收款／交付。

试点建议接单期限 3 分钟，备餐默认值由商家设置。这是可调参数，需现场验证。通知发出不等于店主已读；前台订单列表刷新、明确未接单倒计时和服务端超时任务都必须存在。

### 7.3 状态与异常

| 维度 | 核心状态与规则 |
|---|---|
| 订单 | 等待接单 → 已接单 → 备餐中 → 可取餐 → 已完成 |
| 接单前终止 | 顾客取消、店主拒单、超时关闭均为终态，不能再接单 |
| 接单后取消 | 顾客申请取消 → 商家处理；不能仅凭顾客按钮自动宣告退款完成 |
| 付款 | 待付款／商家确认收款／退款处理中／已退；现金收款记录应注明是商家确认 |
| 交付 | 取餐码只用于该订单；核验后标记交付，重复提交不得重复处理 |

订单保存下单时的菜名、规格、单价、数量、费用、菜单版本及商户信息快照。以后改价或菜单下架不更改历史订单。

服务端按菜品 ID 与合法选项重算金额，使用 THB 最小货币单位整数；拒绝客户端篡改价格。价格已变化时要求顾客重新确认。提交带幂等键，双击、弱网重试不会生成多单。

## 8. 第三方配送阶段

### 8.1 配送商调查

Lalamove 泰国官网提供 API 接入与 Sandbox 步骤，可以先验证。GrabExpress 泰国作为候选，API 商务接入、账户资格、食品承运与费用须另行确认。“滴滴”暂不列为已可接入方案，先确认泰国当地具体可用服务。[Lalamove 泰国 API](https://www.lalamove.com/th-th/business/api-solutions)、[GrabExpress 泰国](https://www.grab.com/th/express/business/)

决定供应商前核实：试点区域覆盖、摩托车／保温需求、食品与液体规则、报价有效期、等待费、取消费、无人接单、收件人失联、赔付条件、企业账户充值与付款人、是否支持代收货款及结算周期。普通配送 API 不等于食品平台、餐费代收或现成退款系统。

### 8.2 顾客额外信息

仅选择配送时要求收件人、电话、文字地址、地图落点，楼层／门牌按需补充。店主的取货地点、电话、入口说明只设置一次。超范围直接解释原因并允许改为自取。

### 8.3 配送订单流程

1. 顾客选配送、填写地点；系统请求运费报价，展示餐费、运费和合计。
2. 顾客确认金额，提交等待商家接单的申请。
3. 商家确认可做，检查报价是否仍有效；失效则重新报价，增加费用必须重新取得顾客确认。
4. 满足商家确认、有效价格确认和约定收款条件后，服务端创建配送单。
5. 等待骑手分配，并与商家约定的备餐节奏配合；初期未确认运力前不通知商家开始不可逆制作。
6. 已分配 → 到店取货 → 配送中 → 已送达；顾客、商家看到同步状态。
7. 无运力或创建失败时提供明确选择：等待、改自取、取消；已备餐、已收款订单转人工处理，不能静默重派。

订单、付款和配送是独立状态，不能把“创建配送成功”当作“商家接单”“已付餐费”或“已送达”。

### 8.4 技术与资金要求

- 供应商接口封装为报价、创建、查询、取消、处理回调五个动作；首期仅实现一家真实供应商。
- 保存报价 ID、过期时间、费用明细、配送单 ID、取消原因和实际费用；按返回值判断，不硬编码所有市场能力。
- 创建超时可能已经派单成功：先查单或核对供应商记录，再决定重试。无法确认时人工排查，禁止盲目重复叫车。
- 回调按供应商支持的认证机制校验，去重并处理乱序；缺少可信认证时主动查单确认关键状态。状态回调丢失有定时对账补偿。
- 密钥仅在服务端；配送商只获得本次履约需要的信息。
- 餐费、运费、服务费（若以后存在）、等待费与取消费分别记录。真实开通前决定谁收餐费、谁向配送商付费、谁承担取消费、谁退款。
- 若采用商家直接收款，付款截图仅是辅助凭证，不能自动视为付款成功；需商家核对或可验证支付回调。
- 不默认支持骑手代收。若平台企业账户先付运费，必须预算余额、坏账与退款成本，不能假设“免费工具”没有现金支出。

Lalamove 官方接口包含报价有效期、创建订单与状态回调，可据此设计测试适配层；实际授权与地区能力以接入结果为准。[Lalamove API 文档](https://developers.lalamove.com/)

## 9. LINE 通知与接单可靠性

现有 LIFF 登录成功不等于已经能够主动发新订单通知。接单阶段需单独配置 LINE Official Account 与 Messaging API，确认收件资格、用户关联、额度和商家是否屏蔽账号。用户 ID 的关联需核对 Provider，不能按显示昵称匹配。[LINE 推送消息接口](https://developers.line.biz/en/reference/messaging-api/#send-push-message)

第一轮真实试点要求商家开启接单页面，并验证前台提示；手机锁屏或切后台不能依赖网页持续执行。新增通知有重试记录，超时逻辑由持久任务处理，不能依赖顾客页面计时器。商家暂时收不到通知时应停止接单或启用已验证的备用联系方法。

顾客订单页提供刷新后的最新状态。通知失败不撤销已提交订单，页面明确“订单已提交，提醒发送失败”，后台可追踪并补发。

## 10. 责任与数据边界

“顾客和商家自行解决纠纷”可以描述主要协商路径，但不能作为平台零责任的保证。ETDA 曾明确将 Food Delivery 平台问题纳入 DPS 监管讨论；我们是否适用哪些具体义务取决于实际经营、合同、收款与派单安排，需在泰国正式试运营前核实。[ETDA 官方说明](https://www.etda.or.th/th/pr-news/food_Delivery_problem.aspx)

建议分工：商家处理商品内容、价格、制作和食品问题；配送商按合同处理运输；平台处理身份与系统故障、订单记录、投诉受理及转交，并按实际合同参与取消和退款协调。不能用条款预先认定所有情况下均无责任。

对外试点至少落实商户身份与联系方式、订单条款、取消规则、投诉入口和订单操作记录。由当地专业人士核查平台登记、消费者保护及个人数据处理要求；本文件不作适用性法律结论。

数据目标优先是经校对的菜单、营业规律、订单量、售罄与取消原因。联系信息和配送地址只为履约与必要售后使用；控制访问、保存期限和删除流程。营销授权与完成订单分开，分析优先聚合，不能把消费者地址和 LINE 身份默认当作可出售资产。

## 11. 数据模型与架构建议

| 实体 | 关键内容 |
|---|---|
| Merchant / Ownership | 稳定商户 ID、所有者、行业、审核与暂停状态 |
| MerchantProfileRevision | 基础资料工作版本与已批准版本 |
| BusinessSchedule / Override | 时区、周时段、日期例外、暂停、当班接单、自取／配送开关 |
| MenuImportJob | 图片引用、任务状态、识别结果、原始来源、校对记录 |
| MenuVersion / Item / Option | 已发布版本、菜品、合法选项、价格、售罄及上架状态 |
| Customer | 经验证的顾客身份与必要联系信息 |
| Order / OrderItem | 商户与顾客、价格快照、状态、幂等键、有效期、取餐凭据 |
| DeliveryQuote / Delivery | 报价、授权金额、配送单、事件、费用与异常 |
| PaymentRecord | 支付主体、方式、状态、外部引用与退款记录 |
| OrderEvent / NotificationJob | 操作者、事件、通知重试、超时任务 |
| SupportCase | 投诉、转交对象、处理结果与相关订单 |

建议在真实接单前引入 PostgreSQL 等支持事务的数据库，用于订单、菜单版本和状态变更；图片继续放对象存储。当前 Netlify Blobs 存储层没有足够的跨记录事务机制，不能直接复制商户 JSON 写法承担并发订单。

迁移必须保留现有商户 ID、归属、审核状态与公开 URL。流程为：备份 → 在测试环境转换与核对数量／图片引用 → 短暂暂停相关写入 → 导入并复核 → 切换唯一数据源 → 验证。切换失败可在没有新写入前回退；已经产生新订单后需先对账，不能直接切回旧快照。

商户、顾客与平台三类权限都在服务端按订单归属校验。取餐码不允许用作公开查询全部订单的凭证；订单 URL 也不替代登录授权。

提交订单、菜单版本检查、接单或超时竞态由事务与条件更新处理。任务使用持久化队列／任务表与可重试执行器；不在无持久保障的服务器后台随手启动 OCR 或派单。数据库事务不能覆盖第三方 API，派单与通知需可重试、可查证、可补偿。

## 12. 分阶段实施与验收

以下是依赖顺序，不是工期承诺。每一阶段先达到退出条件再启用下一阶段。

| 阶段 | 交付内容 | 退出条件 |
|---|---|---|
| A：数据与规则设计 | 菜单、营业、订单模型；数据库迁移方案；费用和角色规则 | 现有商户归属与 URL 可保留；状态规则能覆盖休店、超时和取消 |
| B：OCR 菜单发布 | 多图上传、识别任务、原图校对、版本发布、菜单链接／二维码、售罄 | 至少 20 张菜单基准测试；错误价格被标记且可修正；未确认结果不公开 |
| C：自取闭环 | 周营业表、今天休息、当班接单、购物车、顾客认证、接拒单、取餐码、通知和订单记录 | 两个设备同步完整自取；重复点击只一单；超时无人接单可恢复且有结果 |
| D：配送 Mock / Sandbox | 报价、地址、派单、取消、失败模拟、回调与对账 | 全部配送异常用例通过；测试不会真实扣款或叫车 |
| E：真实配送试点 | 一家服务商、限定区域、资金方案、投诉与支持流程 | 合同与账号可用；可控成本下完成真实配送及一次异常处理演练 |
| F：后续优化 | 根据试点改录入、接单与履约；再研究真实页面 SEO 效果 | 有实际使用指标支持下一轮投入 |

第一轮可运行版本建议交付 B + C 的 Mock 演示，并把 D 做成清晰标注的模拟选项；外送最终需求保持在计划内。真实消费者只看到该店已开通、可兑现的方式。

下一次开发最小任务包：在已有商户中加入“菜单管理”，完成上传 → Mock 识别 → 店主修正 → 持久保存并发布 → 手机公开菜单查看。随后接真实 OCR，再进入可提交订单的阶段。Mock 识别会提供固定、可复现样例，不冒充可以识别任意照片。

## 13. Mock 与验收数据计划

Mock 只替代 OCR、支付与配送供应商，不替代核心订单规则。跨手机与电脑测试仍走隔离的服务器持久数据；演示订单不得混入正式订单、公开索引或真实配送队列。

开发阶段再产出 `test-fixtures/ordering/` 下的菜单图片、预期结果和场景说明；本次仅创建计划，不声称样例文件已存在。模拟数据明确标识，仅管理员可以重置测试空间。

| 场景 | 预期结果 |
|---|---|
| 菜单反光、缺价、大小份同列 | 待确认标识，不生成可下单的猜测价格 |
| OCR 超时、切后台、重新进入 | 可查任务状态和恢复校对，不重复扣识别额度 |
| 同图重传 | 显示已有任务或差异，不覆盖已发布菜单 |
| 改价时顾客正在结算 | 重新确认新价格；已成立订单保持旧快照 |
| 顾客双击、弱网重试 | 同一个幂等请求只产生一个订单 |
| 店主与顾客两台设备 | 同步同一服务器记录，不依赖本地商户数据 |
| 点击今天休息后顾客提交 | 服务端拒绝新单，解释休店原因 |
| 周例休、日期例外、跨午夜 | 按曼谷时间给出正确状态；次日不误开接单 |
| 商家锁屏、提醒失败、无响应 | 服务端超时关闭或明确处理，顾客不无限等待 |
| 接单与超时同时发生 | 只允许一个合法结果，事件记录可追踪 |
| 提交前售罄、关闭配送 | 重新校验并拒绝无效选项，不自动改成自取 |
| 非本人查看订单或改他店营业 | 服务端拒绝，不泄露联系方式 |
| 配送超范围、报价过期、涨价 | 解释原因并重新确认，不能自动多扣款 |
| 叫车请求超时但供应商成功 | 查单后关联已有配送，不创建第二个骑手任务 |
| 回调重复、乱序、伪造或丢失 | 不重复完成、不回退终态；可查单与对账 |
| 无骑手、顾客不在、取餐后取消 | 进入明确异常处理，不伪造已完成／已退款 |
| 更新商户资料、重新部署 | 已发布内容按版本规则保留；已有订单和任务仍可查 |

Mock 能验证流程、计算和状态规则。OCR 质量必须通过实际识别测试；通知可达性、商家接单习惯、真实骑手时效、实际取消费用和顾客是否愿意下单，都需要小规模现场验证。

## 14. 试点指标与成本

首批选择 3–5 家愿意接单、有人看手机的餐饮商户；先自取，再在一个区域开放配送。

- 菜单：上传到发布时间、校对耗时、价格修正数、每张识别成本。
- 订单：进入菜单到提交比例、接单时间、超时率、完成率、重复订单数。
- 营业：休店误接单数、售罄拒单数、忘记打开接单次数。
- 配送：报价失败率、派单失败率、交付时间、取消与等待费。
- 运营：每单人工介入分钟数、投诉原因、商家一周后是否继续接单。
- 数据：字段完整度、确认来源、更新时间、可聚合分析程度。

免费不等于没有成本。分别核算 OCR／模型、数据库、图片、通知、配送垫付、支付与退款、客服；单均成本按已完成订单计算，同时记录失败订单消耗。数据积累是潜在资产，不能先当作已实现收入抵消现金支出。

## 15. 真实外送前需要负责人确定的事项

1. 首个城市／配送区域，首批餐饮商户名单与试点负责人。
2. 餐费与运费分别由谁收、谁付；是否接受平台垫付以及预算上限。
3. 顾客取消、商家拒单、已备餐、骑手已取货等节点的费用承担与退款处理。
4. 配送合作账号归属、食品配送许可范围、服务协议及联系人。
5. 订单投诉与系统故障由谁接手，营业时间内如何联系到负责人。
6. 是否接受首次试点提交订单需要 LINE 登录；不能登录的人走什么替代路径。
7. OCR／通知／数据库预算与数据保留、授权及法律核查结果。

这些决定不妨碍先实现菜单识别、公开菜单和隔离 Mock 订单；它们是开放真实配送与资金处理的前置条件。
