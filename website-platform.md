# 独立网站模式与账号迁移

更新：2026-09-13。当前产品入口为网站，不要求 LINE 客户端、LINE 账号或 MINI App 权限。

## 使用方式

1. 在普通手机浏览器或电脑打开网站首页；中泰英界面均可使用。
2. 点击「账号」或菜单入口，注册用户名与密码。用户名为 3–40 位字母、数字、下划线或短横线，不区分大小写；密码为 12–128 位。
3. 新商家填写基本资料并提交审核；一个账号默认一家商户。已存在旧 LINE 商户时不要重复建店。
4. 在「创建线上菜单」中上传、模拟识别、校对与保存；审核通过后可发布。
5. 顾客通过公开菜单链接查看，无需账号。目前尚不能下单。

首页始终显示菜单工具，未登录时进入账号登录提示，而不是隐藏菜单入口。

## 旧 LINE 商户

- 原始商户、图片、菜单和公开 URL 保持原样，不批量删除或重新创建。
- 店主先注册一个尚未创建商户的网站账号。
- 管理员在 `/admin/login` 登录原管理后台，核实店主身份后，找到旧商户并点击「绑定网站账号」。
- 输入店主网站用户名并确认。绑定的是该旧 LINE 所有者名下的全部历史商户，历史重复记录仍按原界面处理。
- 店主重新登录或刷新网站，原资料即可加载。一个旧所有者不能被两个网站账号同时认领；已有新商户的账号不能绑定旧资料。
- 系统不按手机号、店名或用户输入的 LINE ID 自动认领。旧服务端 LINE token 认证暂作兼容；网站前端不加载 LINE SDK。
- 绑定流程为管理员操作，含条件写入与账号归属预留。存储中断时按原账号和原商户重试；不要改绑给另一账号来绕过冲突。

## 数据与身份

- 账号使用 Netlify Blobs 的独立存储 `merchant-launchpad-accounts-v1`；菜单与商户仍使用原站点存储。
- 密码仅保存带随机盐的 scrypt 派生值，不保存明文密码。
- 登录会话为随机令牌，服务端仅保存令牌摘要，14 天过期；Cookie 为 HttpOnly、SameSite=Strict，HTTPS 下为 Secure。退出会删除服务器会话。
- 网站写请求检查 Origin、跨站标记与服务器身份；商户归属及管理员权限仍由服务端验证。
- 登录注册有持久化尝试次数限制；初期账号限制 12 次／15 分钟，来源地址限制 100 次／15 分钟。未来应接入更完整的边缘限流、自动清理过期记录及账号恢复。
- 当前没有邮箱验证、忘记密码、多因素认证或自助改密码。请保管密码；扩大公开运营前应补齐账号恢复、滥用治理及数据保留机制。
- 当前不需要额外认证服务或新增部署密钥。保留原 `ADMIN_API_KEY`，生产仍禁止 `MERCHANT_STORAGE=memory` 和 `ALLOW_MOCK_LINE_AUTH=true`。

## 已验证与待验证

本地验证包括独立网站注册／登录、Cookie 会话、退出失效、错误密码、限流、跨账号拒绝、跨来源拒绝、单账号单商户、旧资料管理员绑定、菜单校对发布和手机尺寸页面；浏览器流程不发起 LINE 请求。

本地测试使用独立临时数据。上线后仍需用自己的账号检查双设备持久保存、实际手机上传与 Netlify 数据存储连接。菜单识别仍是固定 Mock 样例，不是真实 OCR。
